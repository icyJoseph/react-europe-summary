{
  "name": "react-meetup-summary",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "mdx-deck deck.mdx",
    "build": "mdx-deck build deck.mdx",
    "deploy": "yarn build && surge --domain https://puny-fog.surge.sh/ --project ./dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "mdx-deck": "^2.3.2"
  },
  "dependencies": {
    "@mdx-deck/export": "^2.3.2",
    "react-syntax-highlighter": "^10.3.0"
  }
}
